console.log("utils.js");

const name1 = "Isaac";

const add = function (a, b) {
  return a + b;
};

//export variable for global directory scope
module.exports = add;

//Use type "clear" in terminal to clear
